{
  "name": "{{projectName}}",
  "version": "{{projectVersion}}",
  "scripts": {
    "postinstall": "npm dedupe",
    "test": "mocha"
  },
  "dependencies": {
    "webfunc": "^0.4.0-alpha.0"
  },
  "devDependencies": {
    "chai": "^4.1.0",
    "colors": "^1.1.2",
    "mocha": "^3.4.2",
    "shelljs": "^0.7.8"
  }
}
